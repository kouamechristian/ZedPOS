{% extends 'admin/base.html.twig' %}
{% form_theme form 'admin/_form_theme.html.twig' %}

{% block page_title %}{{ titre }}{% endblock %}

{% block admin_content %}
<div class="max-w-2xl bg-white rounded-xl shadow-sm p-6">
    {{ form_start(form) }}
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-5">
        <div class="sm:col-span-2">{{ form_row(form.nom) }}</div>
        {{ form_row(form.familleProduit) }}
        {{ form_row(form.unite) }}
        {# Champ absent du formulaire si l'utilisateur n'est pas habilité au prix. #}
        {% if form.prixVenteTtc is defined %}
            {{ form_row(form.prixVenteTtc) }}
        {% else %}
            <div class="mb-4">
                <span class="block text-sm font-medium text-slate-700 mb-1">Prix de vente TTC</span>
                <p class="rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-500">
                    {{ article is defined ? (article.prixVenteTtc / 100)|number_format(0, ',', ' ') ~ ' FCFA' : '—' }}
                    <span class="block text-xs">Seule la dirigeante peut fixer un prix de vente.</span>
                </p>
            </div>
        {% endif %}
        {{ form_row(form.tauxTva) }}
        {{ form_row(form.positionCaisse) }}
        {{ form_row(form.couleur) }}
    </div>
    <div class="mt-2">{{ form_row(form.actif) }}</div>

    <div class="mt-6 flex items-center gap-3">
        <button class="rounded-lg bg-slate-800 text-white px-4 py-2 text-sm font-semibold hover:bg-slate-700">Enregistrer</button>
        <a href="{{ article is defined ? path('admin_article_show', {id: article.id}) : path('admin_article_index') }}"
           class="rounded-lg border border-slate-300 px-4 py-2 text-sm hover:bg-slate-50">Annuler</a>
    </div>
    {{ form_end(form) }}
</div>
{% endblock %}
