{# Par vendeur : CA généré, rémunération, écarts, dette actuelle — celle d'aujourd'hui, pas celle de la période. #}
{{ include('admin/rapport_stand/_groupes.html.twig', {nature: 'vendeur', titre: 'Vendeur', dette: true}) }}
